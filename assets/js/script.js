var currentDate = moment().format("dddd" + ", " + "MMMM DD");

// add current day and date to header
$('#currentDay').text(currentDate);

// get current time
var currentTime = moment().format("hh a");
console.log(currentTime);

// get task time

// compare current time to task time

